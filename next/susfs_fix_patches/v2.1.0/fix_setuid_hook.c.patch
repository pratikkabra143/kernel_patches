diff --git a/kernel/hook/setuid_hook.c b/kernel/hook/setuid_hook.c
index d3aef3c1..63aee561 100644
--- a/kernel/hook/setuid_hook.c
+++ b/kernel/hook/setuid_hook.c
@@ -93,6 +93,14 @@ int ksu_handle_setresuid(uid_t ruid, uid_t euid, uid_t suid)
         ksu_clear_task_tracepoint_flag_if_needed(current);
     }
 
+    if (unlikely(new_uid == 0)) {
+        susfs_handle_setresuid(old_uid, new_uid, 0);
+        goto do_umount;
+    }
+
+    return 0;
+
+do_umount:
     // Handle kernel umount
     ksu_handle_umount(old_uid, new_uid);
 
